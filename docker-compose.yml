services:
  jarvis:
    build: .
    container_name: jarvis
    restart: unless-stopped

    ports:
      - "8001:8000"

    environment:
      OLLAMA_HOST: http://172.20.0.1:11434
      OLLAMA_MODEL: qwen2.5:3b

    networks:
      - homelab

networks:
  homelab:
    external: true
